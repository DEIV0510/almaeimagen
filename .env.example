# Copia este archivo como .env y rellena los valores reales.

# PostgreSQL (Neon/Vercel). Usa la conexión DIRECTA (la variable DATABASE_URL_UNPOOLED de Vercel).
DATABASE_URL_UNPOOLED="postgresql://usuario:password@host/basededatos?sslmode=require"

# Clave secreta para firmar las sesiones (genera una cadena larga y aleatoria).
JWT_SECRET="cambia-esto-por-una-clave-larga-y-unica"
